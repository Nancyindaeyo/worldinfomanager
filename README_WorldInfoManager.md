# 世界书批量管理工具 (World Info Batch Manager)

这是一个用于 SillyTavern (酒馆) 的增强脚本，旨在提供便捷的世界书条目批量管理功能。它解决了原生界面无法批量操作条目的痛点，采用了无模态框的嵌入式设计。

## ✨ 主要功能

*   **✅ 批量选择**
    *   在每个世界书条目的最左侧直接嵌入复选框。
    *   提供「全选/取消全选」按钮，快速操作。
    *   完美融入原生 UI，不遮挡原有内容。

*   **🗑️ 批量删除**
    *   一键删除选中的多个条目。
    *   操作前会有二次确认提示，防止误删。

*   **➡️ 批量转移 (复制/移动)**
    *   支持将选中条目批量**复制**到另一本世界书。
    *   支持将选中条目批量**移动** (剪切) 到另一本世界书。
    *   **自动跳转**: 转移操作完成后，视图会自动切换到目标世界书，方便立即查看结果。

## 📖 使用说明

1.  **打开世界书**: 在酒馆中打开世界书编辑器（World Info）。
2.  **选择条目**:
    *   脚本会自动加载，你会在顶部看到一个新的控制栏。
    *   勾选条目左侧的复选框，或者点击顶部的「全选」按钮。
3.  **执行删除**:
    *   直接点击红色的「删除选中」按钮即可。
4.  **执行转移**:
    *   点击「批量转移...」按钮，展开转移面板。
    *   在下拉框中选择**目标世界书**。
    *   点击「仅复制」或「移动(剪切)」按钮开始执行。
    *   完成后页面会自动跳转到目标世界书。

## 🛠️ 技术特性

*   **非侵入式设计**: 使用 jQuery 动态注入 DOM，不修改酒馆核心代码。
*   **样式修复**: 针对酒馆条目使用了强制 Flex 布局 (`display: flex !important`)，确保复选框在各种主题下均可见且不破坏原有布局。
*   **智能兼容**: 自动识别世界书的显示名称与内部 ID，解决因酒馆版本差异导致的 "找不到世界书" 错误。
*   **实时监听**: 使用 MutationObserver 监听列表变化，确保在新增条目或切换页面时复选框始终存在。

## ⚠️ 注意事项

*   **删除不可撤销**: 批量删除操作一旦执行无法撤回，请谨慎操作。
*   **移动操作**: "移动" 等同于 "先复制到目标书，再从当前书删除"，如果复制过程出错，原条目不会被删除。
